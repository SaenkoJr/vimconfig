snippet hndlclk "handleClick (...)"
  handleClick = () => {
    ${0}
  }

snippet hndlinpt "handleInputChange (...)"
  handleInputChange = (${1:e}) => {
    ${0}
  }

snippet tag "<tag...>"
  <${1:tagName}${2}>
    ${0}
  </$1>
